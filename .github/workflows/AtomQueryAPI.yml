# This is a basic workflow that is manually triggered

name: Manual workflow to call an API

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  apiCall:
    runs-on: ubuntu-latest
    steps:
    - name: AtomQuery
      id: myRequest
      uses: fjogeleit/http-request-action@master
      with:
        url: 'https://api.boomi.com/api/rest/v1/neosalpha-SSL1AK/Atom/query'
        method: 'POST'
        contentType: 'application/json'
        data: '{"QueryFilter" : {"expression" :{"argument" : ["ATOM"], "operator" : "EQUALS", "property" : "type"}}}'
        username: BOOMI_TOKEN.kartik.dashora@neosalpha.com
        password: f0f0c5d7-bd36-4aea-b0b9-4950a7e8c976
    - name: Show Response
      run: echo ${{ steps.myRequest.outputs.response }}
