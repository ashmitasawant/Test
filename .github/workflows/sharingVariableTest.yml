name: "We 🎔 Perl"
on:
  workflow_dispatch

jobs:
  job1:
    runs-on: ubuntu-latest
    
    steps:
    - id: step1
    - run: echo "Hii"
    - name: Archive code coverage results
      uses: actions/upload-artifact@v2
      with:
          name: code-coverage-report
          path: ProcessDetails_CSV.csv
    - run: echo "All!"
  
  
